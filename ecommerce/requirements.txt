# Redis Cloud Context Retriever SDK  (replaces hand-written MCP server)
context-surfaces>=0.0.5          # installs redis-context-retriever + ctxctl CLI

# LangGraph agent
langgraph>=0.2.0
langchain-openai>=0.1.0
langchain-core>=0.2.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0

# Only needed for setup_data.py (vector embeddings for the separate idx:ecommerce index)
redis>=4.6.0
sentence-transformers>=2.7.0
numpy>=1.26.0
